{
  "name": "college-attendance-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "mobile:sync": "vite build && npx cap sync",
    "mobile:open": "npx cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^8.1.0",
    "@capacitor/core": "^8.1.0"
  }
}